'use strict';

/**
 * ai-training-session service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ai-training-session.ai-training-session');
