'use strict';

/**
 * ai-training-session controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ai-training-session.ai-training-session');
