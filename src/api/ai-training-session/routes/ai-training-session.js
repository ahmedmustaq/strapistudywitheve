'use strict';

/**
 * ai-training-session router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ai-training-session.ai-training-session');
